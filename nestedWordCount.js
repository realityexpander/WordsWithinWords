function nestedWordCount (wordList) {


// gray -> ray, gray
// grays -> gray, grays, ray, rays 
// ray -> ray
// rays -> ray, rays
// strays -> ray, rays

// For each word
  // check aaginst all other words
    // does this word contain the other word?
      // yes -> increment count for this word

}
